describe('AuthController', () => {
  describe('POST /api/v1/auth', () => {
    test.todo('should return a valid token');
    test.todo("the token payload should contain the user's profile");
    test.todo('the token should be valid for 30 days');
  });

  describe('DELETE /api/v1/auth', () => {
    test.todo('tokens should no longer be valid');
  });
});
